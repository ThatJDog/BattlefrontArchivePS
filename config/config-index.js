// Convert all files again (quicker way to get the .sri to .pdml)
// Update file collector / Pre-process data (into .json)

// Should pre-process calculating the man of the matches for example
// For every series entry, add a man-of-the match or award entry
// for the best player in that match. (Card rating can be their match score)